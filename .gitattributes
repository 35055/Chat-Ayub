# Принудительно LF в репозитории
* text=auto eol=lf

# Бинарники — не трогаем (Git не меняет переносы строк)
*.jpg binary
*.png binary
*.gif binary
*.ico binary
*.pdf binary
*.exe binary
*.dll binary
